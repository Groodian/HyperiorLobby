name: $name
version: $version
main: de.groodian.hyperiorlobby.main.Main
description: $description
load: STARTUP
authors:
  - $author
api-version: $apiVersion

depend: [ HyperiorCore ]

commands:
  lsetup:
    description: Setting up the lobby
  build:
    description: Build mode